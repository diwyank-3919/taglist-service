version: "3.8"

services:
  taglist-service:
    build: .
    container_name: taglist-service
    ports:
      - "3000:3000"
    restart: unless-stopped
    # Chromium needs more shared memory than Docker's tiny default
    shm_size: "1gb"
